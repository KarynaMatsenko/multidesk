export enum Role {
    User,
    Admin,
}

export enum LoginActionTypes {
    LOGIN = 'LOGIN',
    LOGOUT = 'LOGOUT',
}

export enum AdminWindowActionTypes {
    ENTER_SETTINGS = 'ENTER_SETTINGS',
    EXIT_SETTINGS = 'EXIT_SETTINGS',
}
